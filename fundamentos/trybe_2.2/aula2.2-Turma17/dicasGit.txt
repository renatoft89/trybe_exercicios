Dicas Git:

Não esquecer ⚠️

UM REPOSITÓRIO REMOTO NO GITHUB É ONDE SÃO GUARDADOS OS SEUS ARQUIVOS NA NUVEM.
JÁ O REPOSITÓRIO LOCAL É ONDE SE GUARDA OS SEUS ARQUIVOS NA SUA MÁQUINA.

Resumo dos comandos: 🙂

git clone - Serve para "fazer o download" de um repositório remoto para a sua máquina. Imaginando que um repositório é uma caixa com coisas dentro, ele faz uma cópia exata da caixa que está remota e te dá acesso local nessa cópia.

git branch - Se usa este comando para ver as branchs de um repositório que existem na sua máquina. Uma branch é uma cópia da caixa principal. Ela serve para você fazer alterações sem impactar o conteúdo original. Este comando mostra todas as caixas que existem acessíveis na sua máquina local. Assim você fica com a caixa principal e com as caixas com alterações, cada uma sendo uma branch.

git branch nome-da-nova-branch - Serve para criar uma nova branch. Utilizando esse comando você cria uma nova caixa, que é uma cópia da caixa em que você está neste momento. Fica estranho criar tantas caixas assim, cópias e cópias, mas a idéia aqui é justamente ter uma caixa pra cada alteração que você for fazer (pense em uma caixa para cada demanda que for adereçar). É importante nomear as caixas corretamente para que, assim, só de olhar para o nome da caixa você já saiba o que tem dentro.

git checkout nome-da-branch - É usado para acessar (apontar para) uma branch específica. Essa branch pode ser tanto uma nova quanto alguma outra branch já existente no repositório local ou remoto. Este comando serve para você trocar a caixa com a qual está trabalhando, já que você pode ter várias caixas diferentes. Você pode trocar sempre que quiser, seja pra conferir alguma coisa, seja para manter várias alterações em paralelo.

git pull - Serve para puxar para a sua máquina mudanças feitas ao código salvas no repositório remoto, que no nosso caso está no GitHub. Esse comando serve para que você possa atualizar o conteúdo de suas caixas, sincronizando-as com a versão mais recente da caixa que está no repositório remoto. Em um projeto pode existir mais de um colaborador, então imagine que um outra pessoa conseguiu terminar de resolver um bug, ou criar uma melhoria: ele vai jogar essa melhoria na caixa principal. Você pode utilizar o git pull e puxar essas alterações para as suas caixas.

git add . / git add path/to/file - Utiliza-se esse comando para colocar as mudanças no index, que é como se fosse um aeroporto: As mudanças ainda não saíram para a nuvem, mas já estão prontas para isso. O git add pega as mudanças que você fez e coloca num espaço chamado staging area, que é um espaço onde as alterações esperam pra ser empacotadas e enviadas.

git commit -m "mensagem" - Serve para juntar as mudanças do índice num pacote de mudanças (chamado de commit) e inseri-las no repositório local. Normalmente esse comando é utilizado depois do git add, porque ele pega as alterações que foram colocadas na staging area, empacota e coloca como alteração consolidada do repositório local.

git push - Serve para passar as mudanças para o repositório remoto. Você "faz o upload" das mudanças da sua máquina para o repositório remoto.

Merge Pull Request - Serve para agregar as mudanças feitas pela sua branch a uma outra branch. Quando se quer oficializar uma alteração no código, faz-se o merge da branch deste código com a branch maste